# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts
tsconfig.tsbuildinfo

# production
/build

# env
.env
.env*.local

# misc
.DS_Store
*.pem
*.key
npm-debug.log*

# local reference screenshots (visual comparison only — not repo assets)
/img/

# editor
.vscode/*
!.vscode/extensions.json
.idea

# local audit/working artifacts (not repo assets)
/Auditoria_Unpacked/
audit_live_site.py

# agent working artifacts (scraping, data dumps, reference files) — not repo assets
/_scratch/

# downloadable PDF assets — served locally for dev/review, real serving via Phase 3 storage
/PDF/
/public/descargas/

# local graph/code visualization artifacts
.graphifyignore
.opencode/
graphify-out/
