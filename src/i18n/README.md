# i18n (Phase 4)

UI string translation with **next-intl** lands in Phase 4. CMS content translation
(es/pt) is handled separately by Payload's per-field `localized` flag (Phase 3).

This folder will hold `es.json` / `pt.json` message catalogs and the request config.
The skeleton ships Spanish-only UI, matching the original site.
